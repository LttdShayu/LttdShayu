"""
a.首先建立一个简易的crm系统，包含以下特点：
1.在企业管理账号里每个客户的信息只需要{姓名，年龄，性别，客户关系/状态}
2.在每个客户的账号里只需要有和企业管理账号聊天的功能即可
3.要有用户登录界面和对应的系统，有账号和密码，分别给客户和企业管理账号登录后不同的结果，进入不同的系统；遇到数据库里没有的账号返回非法提示，拒绝登录

b.接下来根据这个系统的目的和内容进行测试项目的制定，考虑可能出现的各种情况，来测试出其具体性能：
测试类型	                 测试场景描述	                                        预期指标阈值（参考）
并发用户测试	        10个用户同时进行客户查询操作	                       平均响应时间 < 200ms，错误率 = 0%
	                  50个用户同时进行客户数据更新操作	                     平均响应时间 < 500ms，错误率 < 1%
	                  100个用户混合执行查询、新增、删除操作	                平均响应时间 < 1s，系统无崩溃
数据量测试	        数据库中存储10,000条客户数据进行查询测试	             单条查询响应时间 < 500ms，批量导出时间 < 10s
	            数据库中存储100,000条客户数据进行复杂查询（多条件筛选）    	复杂查询响应时间 < 3s，内存占用增长 < 50%
API性能测试	         连续调用1000次客户创建API接口	                        平均响应时间 < 300ms，成功率 100%
	                   连续调用1000次客户删除API接口	                        平均响应时间 < 200ms，成功率 100%
数据库操作	             批量导入1000条客户数据	                          导入时间 < 10s，数据库锁等待时间 < 500ms
	                     复杂报表生成（多表关联查询）	                    报表生成时间 < 15s，数据库CPU占用 < 70%
系统资源监控	           高并发场景下CPU使用率监控	                             峰值CPU使用率 < 80%
	                    内存泄漏测试（连续运行24小时）	                          内存占用稳定增长 < 10%
	                    磁盘I/O监控（批量数据导出时）	                      磁盘读写速度稳定，无长时间I/O等待
边缘场景测试	         超大客户名称（1000字符）的创建与查询	                   系统无崩溃，响应时间 < 1s
	                特殊字符（emoji、Unicode）的客户数据处理	                   数据存储正确，查询结果准确
	                 网络延迟模拟（200ms延迟）下的系统操作	                   响应时间增长 < 50%，无超时错误

c.接着开发这些测试的脚本，并留出给ai的接口，接入ai的api进行各类测试，并给出系统测评：

  <img width="401" height="291" alt="7c4aead08750138a7dabc9095f5f45d" src="https://github.com/user-attachments/assets/0b00b888-5a34-4f4c-9117-6b8c2ea8bdf1" />

<img width="1252" height="337" alt="edc97419e591b563019c919c3afd172" src="https://github.com/user-attachments/assets/1a1b6a59-e4ff-4b19-864b-266a72ef2aef" />

"""
